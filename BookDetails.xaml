<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PaperTrails_ThomasAdams_c3429938.Pages.BookDetails"
             Title="">

    <ContentPage.Resources>
        <Style x:Key="HeadingLabelStyle" TargetType="VerticalStackLayout">
            <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource LightDirt}}" />
            <Setter Property="MinimumHeightRequest" Value="40" />
            <Setter Property="HorizontalOptions" Value="Fill" />
        </Style>

        <Style x:Key="HeadingLabel" TargetType="Label">
            <Setter Property="SemanticProperties.HeadingLevel" Value="Level2" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="HorizontalOptions" Value="Start" />
            <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Main}}" />
            <Setter Property="Padding" Value="5, 6, 0, 5" />
            <Setter Property="HorizontalOptions" Value="Center"/>
        </Style>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout>
            <Image
                Source="{Binding thumbnail}"
                HorizontalOptions="Center"
                HeightRequest="200"
                WidthRequest="300"/>


            <Border BackgroundColor="{AppThemeBinding Light={StaticResource Moss}}" Margin="0, 5, 0, 5" Padding="0, 5, 0, 10" WidthRequest="350">

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="350" />
                </Border.StrokeShape>

                <VerticalStackLayout 
                    Spacing="5"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"
                    WidthRequest="300">
                    <Label
                        Text="{Binding title}"
                        SemanticProperties.HeadingLevel="Level2"
                        FontSize="14"
                        HorizontalOptions="Center"
                        Margin="0, 5, 0, 0"
                        TextColor="{AppThemeBinding Light={StaticResource Text}}"
                        LineBreakMode="WordWrap"/>
                </VerticalStackLayout>
            </Border>

            <Label
                Text="{Binding description}"
                SemanticProperties.HeadingLevel="Level3"
                FontSize="14"
                Margin="30, 5, 20, 10"/>


            <VerticalStackLayout Style="{StaticResource HeadingLabelStyle}">
                <Grid ColumnDefinitions="*, *" Margin="0, 0">
                    <Label
                        Grid.Column="0"
                        Text="{Binding categories, StringFormat='Genre: {0}', FallbackValue='Genre: N/A'}"
                        FontSize="16"
                        HorizontalOptions="Start"
                        Margin="10, 10, 0, 0"
                        VerticalOptions="Center"
                        TextColor="{AppThemeBinding Light={StaticResource Main}}"/>

                    <Label
                        Grid.Column="1"
                        Text="{Binding pageCount, StringFormat='Page Count: {0}', FallbackValue='Page Count: N/A'}"
                        FontSize="16"
                        HorizontalOptions="End"
                        Margin="0, 10, 10, 0"
                        VerticalOptions="Center"
                        TextColor="{AppThemeBinding Light={StaticResource Main}}"/>
                </Grid>
            </VerticalStackLayout>

            <VerticalStackLayout Style="{StaticResource HeadingLabelStyle}">
                <Grid ColumnDefinitions="*, *" Margin="0, 0">
                    <Label
                        Grid.Column="0"
                        Text="{Binding authors, StringFormat='Author(s): {0}', FallbackValue='Author(s): N/A'}"
                        FontSize="16"
                        HorizontalOptions="Start"
                        Margin="10, 5, 0, 5"
                        VerticalOptions="Center"
                        TextColor="{AppThemeBinding Light={StaticResource Main}}" />

                    <Label
                        Grid.Column="1"
                        Text="{Binding publishedDate, StringFormat='Published: {0}', FallbackValue='Published: N/A'}"
                        FontSize="16"
                        HorizontalOptions="End"
                        Margin="0, 5, 10, 5"
                        VerticalOptions="Center"
                        TextColor="{AppThemeBinding Light={StaticResource Main}}"/>
                </Grid>
            </VerticalStackLayout>

            <Button
                Text="Want To Read"
                Margin="0, 20"
                WidthRequest="200"
                HorizontalOptions="Center"
                Clicked="ReadButton_Clicked"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>